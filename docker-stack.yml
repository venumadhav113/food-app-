version: "3.8"



services:
  frontend:
    image: venumadhav113/docker-node:fronttag
    ports:
      - "80:80"
    networks:
      - foodnet
    deploy:
      replicas: 2

  backend:
    image: venumadhav113/docker-node:backtag
    networks:
      - foodnet
    deploy:
      replicas: 2

  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - dbdata:/var/lib/mysql
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - foodnet

volumes:
  dbdata:

networks:
  foodnet:
    driver: overlay
